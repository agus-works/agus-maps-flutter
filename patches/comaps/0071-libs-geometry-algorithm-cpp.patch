diff --git a/libs/geometry/algorithm.cpp b/libs/geometry/algorithm.cpp
index c55e93bcd..20aa27057 100644
--- a/libs/geometry/algorithm.cpp
+++ b/libs/geometry/algorithm.cpp
@@ -1,8 +1,6 @@
 #include "geometry/algorithm.hpp"
 #include "geometry/triangle2d.hpp"
 
-#include "base/logging.hpp"
-
 namespace m2
 {
 // CalculatePolyLineCenter -------------------------------------------------------------------------
@@ -18,13 +16,12 @@ PointD CalculatePolyLineCenter::GetResult() const
 
   double const l = m_length / 2.0;
 
+  if (m_poly.empty())
+    return m2::PointD(0, 0);
+
   TIter e = lower_bound(m_poly.begin(), m_poly.end(), Value(m2::PointD(0, 0), l));
   if (e == m_poly.begin())
-  {
-    /// @todo It's very strange, but we have linear objects with zero length.
-    LOG(LWARNING, ("Zero length linear object"));
     return e->m_p;
-  }
 
   TIter b = e - 1;
 
